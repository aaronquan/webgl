

class RectGrid{
  //grid: boolean[][];

  width: number;
  height: number;
  size: number;
  constructor(w: number, h: number, s: number){
    this.width = w;
    this.height = h;
    this.size = s;
  }
}